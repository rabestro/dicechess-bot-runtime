<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>lv.id.jc</groupId>
    <artifactId>dicechess-bot-runtime</artifactId>
    <version>0.2.2-SNAPSHOT</version>
    <packaging>jar</packaging>

    <name>DiceChess Bot Runtime</name>
    <description>Transport runtime for DiceChess webhook bots: HMAC signing, the ownership handshake, and HTTP wiring
        for the Azure Functions custom-handler model.
    </description>
    <url>https://github.com/rabestro/dicechess-bot-runtime</url>
    <inceptionYear>2026</inceptionYear>

    <licenses>
        <license>
            <name>MIT License</name>
            <url>https://opensource.org/licenses/MIT</url>
        </license>
    </licenses>

    <developers>
        <developer>
            <id>rabestro</id>
            <name>Jegors Čemisovs</name>
            <url>https://github.com/rabestro</url>
            <roles>
                <role>developer</role>
            </roles>
            <timezone>Europe/Riga</timezone>
        </developer>
    </developers>

    <scm>
        <url>https://github.com/rabestro/dicechess-bot-runtime</url>
        <connection>scm:git:https://github.com/rabestro/dicechess-bot-runtime.git</connection>
        <developerConnection>scm:git:https://github.com/rabestro/dicechess-bot-runtime.git</developerConnection>
    </scm>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.release>25</maven.compiler.release>

        <gson.version>2.14.0</gson.version>
        <junit.version>6.1.2</junit.version>
        <assertj.version>3.27.7</assertj.version>

        <maven-compiler-plugin.version>3.15.0</maven-compiler-plugin.version>
        <maven-surefire-plugin.version>3.5.6</maven-surefire-plugin.version>
        <maven-javadoc-plugin.version>3.12.0</maven-javadoc-plugin.version>
        <maven-source-plugin.version>3.4.0</maven-source-plugin.version>
        <maven-gpg-plugin.version>3.2.8</maven-gpg-plugin.version>
        <central-publishing-maven-plugin.version>0.11.0</central-publishing-maven-plugin.version>

        <!-- The first release published through this plugin stays a manual promotion: flip to
             `true` (and set the release profile's `waitUntil` to `published`) once its Central
             Portal validation report has been inspected at least once. -->
        <centralPublishing.autoPublish>false</centralPublishing.autoPublish>
    </properties>

    <dependencies>
        <!-- Envelope parsing only; never appears in the public API (String / java.util.* at every boundary). -->
        <dependency>
            <groupId>com.google.code.gson</groupId>
            <artifactId>gson</artifactId>
            <version>${gson.version}</version>
        </dependency>

        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <version>${assertj.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>${maven-compiler-plugin.version}</version>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>${maven-surefire-plugin.version}</version>
            </plugin>

            <!-- doclint is a hard gate: undocumented or malformed public API fails the build,
                 not just the docs.yml publish step (see CI job "javadoc"). -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-javadoc-plugin</artifactId>
                <version>${maven-javadoc-plugin.version}</version>
                <configuration>
                    <doclint>all</doclint>
                    <additionalOptions>
                        <additionalOption>-Xwerror</additionalOption>
                    </additionalOptions>
                    <quiet>true</quiet>
                </configuration>
                <executions>
                    <execution>
                        <id>attach-javadocs</id>
                        <phase>package</phase>
                        <goals>
                            <goal>jar</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <!-- Central rejects a bundle with no sources jar. Attached unconditionally, the same way
                 the javadoc jar above already is, so `mvn package` alone always produces the complete,
                 publishable artifact set — nothing release-specific to remember on a normal build. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-source-plugin</artifactId>
                <version>${maven-source-plugin.version}</version>
                <executions>
                    <execution>
                        <id>attach-sources</id>
                        <phase>package</phase>
                        <goals>
                            <goal>jar-no-fork</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <!-- Signing and the Central Portal upload activate only under `-P release` (the release/cd
         workflows): a laptop `mvn test`/`mvn package` needs neither a GPG passphrase nor Central
         credentials, and stays exactly as fast as it is today. -->
    <profiles>
        <profile>
            <id>release</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-gpg-plugin</artifactId>
                        <version>${maven-gpg-plugin.version}</version>
                        <executions>
                            <execution>
                                <id>sign-artifacts</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>sign</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>

                    <plugin>
                        <groupId>org.sonatype.central</groupId>
                        <artifactId>central-publishing-maven-plugin</artifactId>
                        <version>${central-publishing-maven-plugin.version}</version>
                        <extensions>true</extensions>
                        <configuration>
                            <publishingServerId>central</publishingServerId>
                            <autoPublish>${centralPublishing.autoPublish}</autoPublish>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

    <!-- No <distributionManagement>: with the release profile's central-publishing-maven-plugin
         extension active, it owns `deploy` outright — bundling jar+sources+javadoc+signatures and
         uploading straight to the Central Portal API — so a distribution repository entry has
         nothing left to route. A plain `mvn deploy` outside `-P release` now fails fast with no
         repository configured, which is the correct behavior: nothing should reach for it outside
         the release automation. Versions already on GitHub Packages (0.1.0 through 0.2.2) stay
         resolvable there regardless — this only changes where NEW versions go. -->
</project>
